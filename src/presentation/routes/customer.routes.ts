import { Router } from 'express';



export class Customerroutes {
    static get routes(): Router {
        const router = Router();

        

        // Definir las rutas
       
        return router;
    }
}
